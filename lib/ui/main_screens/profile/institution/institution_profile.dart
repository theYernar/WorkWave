import 'package:flutter/material.dart';

class InstitutionProfile extends StatelessWidget {
  const InstitutionProfile({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}